# マイドライブ整理 — 統合・配置方針

**更新:** 2026-08-12  
**状態:** 一部承認済み。**実ファイル移動はこのクラウド環境では不可。** 家の Mac 用手順 → `メモ/実行チェックリスト_全部移動_Mac用.md`

関連: `メモ/フォルダー名ルール統一案.md`（`2011_督書類` 型）

---

## 承認済み（2026-08-12）

| 項目 | 決定 |
|------|------|
| 親フォルダ名 | `20_パーソナル` → **`20_生活・家庭`** |
| 自動車のまとめ先 | **`20_生活・家庭 / 204_自動車`** |
| 自動車のサブ | **`2041_保険` / `2042_契約・保証` / `2043_経費`**（不明は `2049_その他`） |
| Google AI Studio | ルートを正。`901` はルートへ統合して削除 |

---

## 1. Google AI Studio（承認済み）

| 項目 | 方針 |
|------|------|
| ルート `Google AI Studio` | **正として残す** |
| `90_… / 901) Google AI Studio` | 中身をルートへ統合し、`901` 削除 |

---

## 2. 自動車 — `204_自動車`（承認済み）

### 作成する構造（サブ分け承認済み）

```
20_生活・家庭/
└── 204_自動車/
    ├── 2041_保険
    ├── 2042_契約・保証
    ├── 2043_経費
    └── 2049_その他          … 上記に当てはまらないものだけ
```

### 集約元 → 行き先

| 現状の場所 | 行き先 |
|------------|--------|
| `201) 稲垣書類 / 211) 自動車保険_20260612` | **`2041_保険`**（フォルダごと or 中身。空なら元削除） |
| `★1督書類 / ★1-4 自動車関連` | 内容で `2041`〜`2043`。不明は `2049`。空なら削除 |
| `★1-1 契約書・保証書` 内の**自動車関連** | **`2042_契約・保証`**。車以外は契約書フォルダに残す |
| 税務・収支側の自動車経費（あれば） | **`2043_経費`** |
| 車検証・ローン・メンテ等で上記以外 | 該当へ。不明は `2049_その他` |

### 実行手順（家の Mac）

1. `20_パーソナル` を `20_生活・家庭` に改名  
2. `204_自動車` と `2041_保険` / `2042_契約・保証` / `2043_経費`（必要なら `2049_その他`）を作成  
3. 上表のとおり移動（コピーではなく移動）  
4. 空になった `211)` / `★1-4` を削除  
5. 契約書フォルダに車以外だけ残っていることを確認  

**サブ分けは確定。** 迷うファイルだけ `2049` または `00_Inbox`。

---

## 3. 学習まわり（未承認）

| 種類 | 行き先案 |
|------|----------|
| 司法試験の復習・理解メモ | `10_司法試験`（ルートの `.gdoc` 3件も） |
| それ以外の学習 | `11_学習` |
| `★1-6 学習` | 振り分け後に削除 |

→ まだ「学習は 10/11 に寄せる」の明示承認待ち。

---

## 4. 実行キュー（承認済みから着手可）

優先順:

1. `20_パーソナル` → `20_生活・家庭`  
2. `204_自動車` 作成＋自動車関係を全部集約（§2）  
3. `901) Google AI Studio` → ルートへ統合し削除  
4. （承認後）`)` → `_` 改名、`★1督書類` → `2011_督書類` など  
5. （承認後）学習の寄せ  
6. **触らない:** `99_開発・プロンプト`

---

## 5. 健康ファイル（未承認のまま）

- `カロリー管理エクセル.xlsx` → `203_健康管理`  
- `WOOP作成表.xlsx` → `20110_旗艦思考` または `290_作成資料`

---

## まだ聞きたいこと（短く）

1. 学習は `10` / `11` 寄せで OK？  
2. 命名の `)`→`_` と `2011_督書類` 改名もこの続きで進めてよいか？
