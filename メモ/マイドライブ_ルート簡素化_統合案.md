# マイドライブ ルート簡素化 — 統合案（承認待ち）

**対象:** マイドライブ直下（2026-08-12 スクショ）  
**更新:** 2026-08-12  
**状態:** **移動・改名は未実施。** 承認後のみ、家の Mac で実行。  
**命名:** 旧 `NNN) 名前` / `★…` と新 `NN_名前` の混在 → 統一は `メモ/フォルダー名ルール統一案.md`

---

## 方針（影響を出さないための線引き）

| 区分 | 扱い |
|------|------|
| **触らない** | `99_開発・プロンプト`（パス依存のプロンプト・Python 想定） |
| **触らない** | `00_Inbox`（受け皿として残す） |
| **中身の統合** | 重複・ルート散在・一時系 |
| **名前の統一** | `)` / `★` をやめ、`番号_名前` に揃える（別メモ） |
| **承認後だけ移動・改名** | 既存ルールどおり |

パスをコードや Cursor ルールに書いている可能性があるものほど、フォルダ名変更・移動は避ける。

---

## 現状（スクショ）

```
マイドライブ/
├── ★）北海道チケット_20260824
├── 00_Inbox
├── 10_司法試験
├── 11_学習
├── 20_パーソナル
├── 30_業務 web コンテンツ管理
├── 90_その他・自動生成/     ← 選択中
│   ├── 901) Google AI Studio
│   ├── 902) 松健
│   └── 旧環境_アーカイブ_20260808
├── 99_開発・プロンプト      ← 触らない
├── Google AI Studio         ← ルートにもある（重複）
├── 刑訴法_Q194-…復習.md.gdoc
├── 民法_無権代理…復習.md.gdoc
└── 民法「代理」重…系的理解.gdoc
```

---

## シンプル統合案（推奨セット）

やることは少数。深い `★` 系や `20_パーソナル` の中身統合は第2ラウンド。名前の `)`→`_` は命名メモと同時に進めてよい。

### 1. Google AI Studio の重複解消

| 現状 | 提案 |
|------|------|
| ルートの `Google AI Studio` | → `90_その他・自動生成 / 901_Google AI Studio` に**中身を統合**し、ルート側を削除 |
| `901) Google AI Studio` | 改名して `901_Google AI Studio` を正とする |

※ もし Cursor / スクリプトがルートの `Google AI Studio` を参照しているなら **現状維持**。心当たりがあれば教えてください。

### 2. ルート直下の司法試験 .gdoc 3件を片付ける

| ファイル | 提案行き先 |
|----------|------------|
| `刑訴法_Q194-…復習.md.gdoc` | `10_司法試験`（またはその中の復習用サブフォルダがあればそこ） |
| `民法_無権代理…復習.md.gdoc` | 同上 |
| `民法「代理」重…系的理解.gdoc` | 同上 |

ルートをフォルダだけにするのが目的。学習ノート扱いにするなら `11_学習` でも可。

### 3. 期限付きフォルダを Inbox / アーカイブへ

| 現状 | 提案 |
|------|------|
| `★）北海道チケット_20260824` | 使い終わっていなければ当面ルート維持可。不要なら `00_Inbox` か `90_…/旧環境_アーカイブ_…` へ |
| `旧環境_アーカイブ_20260808` | `90` 内のまま残す（すでに正しい場所） |

### 4. `90_その他・自動生成` の役割を固定

中身はこの3系統だけにする想定:

- `901_Google AI Studio` … 自動生成・実験出力
- `902_松健` … 個別案件（必要なら後で `20` / `30` へ再振り分け）
- `909_アーカイブ_*` … 古い環境の退避

**やらないこと:** `90` を消す／`99` と統合する。

---

## 意図的に今回やらないこと

- `99_開発・プロンプト` の改名・移動・統合
- `20_パーソナル` の `★` 深い階層の統合（命名の `)`→`_` は命名メモの A〜C で先行可）
- `10` / `11` / `30` の中身の再編成
- `902_松健` の行き先変更（中身未確認のため）

---

## 承認の聞き方

**「推奨セットでOK」** で次を実行対象にする想定:

1. ルート `Google AI Studio` → `901_Google AI Studio` へ統合（ルート側削除）
2. ルートの司法試験系 `.gdoc` 3件 → `10_司法試験`
3. `★）北海道チケット_…` → `00_Inbox`（または不要なら削除指示）
4. `90` 配下の `)` 改名（`901_` / `902_` / `909_アーカイブ_…`）
5. `99` は触らない

あわせて **「命名ルールOK」**（または「命名ルールOK、A〜Cまで改名OK」）があると名前統一も同時に進められる。

---

## 実行前チェック（家の Mac）

1. `99_開発・プロンプト` を動かさない
2. ルート `Google AI Studio` を統合する前に、中身が `901` と被っていないかざっと見る
3. 移動はコピーではなく移動。改名は Drive の「名前を変更」
4. 空フォルダだけ削除

---

## 他メモとの関係

- `メモ/フォルダー名ルール統一案.md` … **名前の形を一本化**
- `メモ/パーソナルフォルダー整理_統合案.md` … `20_パーソナル` 内部の統合 → **第2ラウンド**
- 本メモ … **ルートの重複・散在を先に減らす**
