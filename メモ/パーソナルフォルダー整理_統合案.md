# パーソナルフォルダー整理 — 統合案（承認待ち）

**対象:** マイドライブ / `20_パーソナル` 配下（スクショ: `201) 稲垣書類` → `★1督書類`）  
**更新:** 2026-08-06  
**状態:** **移動は未実施。** 行き先を承認後、家の Mac（Google Drive for desktop）で実行する。

**整理ルール（既存）**
1. 新規は `00_Inbox` へ
2. 振り分け前に行き先を確認し、**承認後だけ**移動
3. 判断できないものは Inbox / 現状のまま残す
4. 行き先目安: `10_司法試験` / `11_学習` / `20_パーソナル` / `21_プライベート` / `30_業務管理` / `90_その他・自動生成` / `99_開発・プロンプト`

---

## 現状（スクショから把握した階層）

```
（パーソナル系）
├── 201) 稲垣書類
│   ├── ★1督書類          ← 今回の主対象
│   ├── ★2家族書類
│   └── ドキュメント
├── 202) 収支情報
├── 203) 健康管理
├── 220) パスワード
└── 290) 作成資料
```

### `★1督書類` の中身

| 種類 | 名前 |
|------|------|
| フォルダ | `★1-0 旗艦思考` |
| フォルダ | `★1-1 契約書・保証書` |
| フォルダ | `★1-2 税務関係` |
| フォルダ | `★1-3 給与明細` |
| フォルダ | `★1-4 自動車関連` |
| フォルダ | `★1-5 体験・経験` |
| フォルダ | `★1-6 学習` |
| フォルダ | `★1-A 写真` |
| フォルダ | `★1-Z 他` |
| フォルダ | `★A 手紙` |
| フォルダ | `★ZZ 過去` |
| フォルダ | `他` |
| 直下ファイル | `WOOP作成表.xlsx` |
| 直下ファイル | `カロリー管理エクセル.xlsx` |

---

## 統合・移動の提案（要承認）

各項目について **A / B / 現状維持** などで答えてください。承認分だけ実行用手順に落とします。

### 提案1 — 分散ファイルの移動

| # | 現状 | 提案行き先 | 理由 |
|---|------|------------|------|
| 1 | `★1督書類` / `カロリー管理エクセル.xlsx` | `203) 健康管理` 直下 | 健康管理と内容が一致。督書類から外す |
| 2 | `★1督書類` / `WOOP作成表.xlsx` | `★1-0 旗艦思考` **または** `290) 作成資料` | 目標・計画系なら旗艦思考、作成物なら 290 |

→ **#1・#2 の行き先はどれ？**（または現状維持）

---

### 提案2 — 「他」「過去」系フォルダの統合

現状で似た役割が3つある:

- `★1-Z 他`
- `他`（プレフィックスなし）
- `★ZZ 過去`

| 案 | 内容 |
|----|------|
| **A（推奨）** | `他` の中身を `★1-Z 他` に移して `他` を削除。`★ZZ 過去` はアーカイブとして残す |
| **B** | `他` と `★1-Z 他` を両方 `★ZZ 過去` に統合し、「他」系は1つに |
| **C** | 現状維持（中身を見てから判断） |

→ **A / B / C どれ？**（中身の一覧が必要なら教えてください）

---

### 提案3 — `ドキュメント` フォルダ

`201) 稲垣書類` 直下に汎用名の `ドキュメント` がある。

| 案 | 内容 |
|----|------|
| **A** | 中身を `★1督書類` / `★2家族書類` に振り分けてからフォルダ削除 |
| **B** | `★1-Z 他` にまとめてから削除 |
| **C** | 現状維持（用途あり） |

→ **A / B / C どれ？**

---

### 提案4 — `★1-6 学習` とルート `11_学習` の関係

| 案 | 内容 |
|----|------|
| **A** | 個人の学習資料はルート `11_学習` に寄せ、`★1-6 学習` は空にしたら削除（またはショートカットのみ） |
| **B** | 督書類内の学習は公的・証明書寄りとして `★1-6` に残し、ノート類だけ `11_学習` |
| **C** | 現状維持 |

→ **A / B / C どれ？**

---

### 提案5 — 上位番号フォルダの役割確認（統合候補の有無）

似て見える／横断しそうなもの:

| フォルダ | 想定役割 | 統合候補 |
|----------|----------|----------|
| `201) 稲垣書類` | 本人の公的・私的書類の親 | —（親として維持推奨） |
| `202) 収支情報` | 家計・収支 | `★1-2 税務` / `★1-3 給与` と役割が近いが、**書類 vs 集計**で分けているなら維持 |
| `203) 健康管理` | 健康・カロリー等 | 提案1のカロリー表をここに寄せる |
| `220) パスワード` | 認証情報 | 維持（他と混ぜない） |
| `290) 作成資料` | 自作シート・資料 | WOOP など作成物の受け皿候補 |

→ **202 と ★1-2/★1-3 は分けたまま？ それとも税務・給与を 202 側へ寄せる？**

---

### 提案6 — 命名の統一（任意・後回し可）

- `★1督書類` の「督」が意図どおりか（「督」＝公的・管理書類？）
- プレフィックスなしの `他` は番号付きに揃えると探しやすい
- `★A 手紙` は `★1-` 系列と番号体系が違う → `★1-B 手紙` 等に揃えるか現状維持か

→ **今はやらない / 命名だけ直す / 中身と一緒に直す**

---

## 推奨セット（まとめて承認する場合）

迷ったらこのセットで進める想定:

1. `カロリー管理エクセル.xlsx` → `203) 健康管理`
2. `WOOP作成表.xlsx` → `★1-0 旗艦思考`（違うなら指示を）
3. プレフィックスなし `他` → `★1-Z 他` に統合し、空フォルダ削除
4. `★ZZ 過去` はアーカイブとして残す
5. `ドキュメント`・`★1-6 学習`・上位 202 統合は、中身確認後の第2ラウンド

→ **「推奨セットでOK」** または番号ごとに修正、で回答ください。

---

## 家の Mac での実行手順（承認後）

1. Google Drive for desktop で該当アカウントのマイドライブを開く
2. `20_パーソナル`（またはスクショと同じ親）を開く
3. 承認済みの項目だけ移動（コピーせず移動）
4. 空になったフォルダを削除
5. このメモの状態を「移動済み」に更新

---

## 未確認・追加で欲しい情報

- `ドキュメント` / `他` / `★1-Z 他` / `★ZZ 過去` の中身一覧（ファイル名だけで可）
- `★2家族書類` 側に同様の重複があるか
- ルートが本当に `20_パーソナル` 直下か（`201)` が別の親の下ならパスを教えてください）
